baseURL = "https://pocketdesk.github.io/"
locale = "en-us"
title = "pocketdesk"
enableGitInfo = false
disableKinds = ["taxonomy", "term"]

[params]
  description = "A pocket-sized Linux dev workstation — your phone is the computer, AR glasses are the screen, a folding keyboard is the input. No laptop."
  docsURL = "/docs/"
  ghOrg = "https://github.com/pocketdesk"

[markup.goldmark.renderer]
  unsafe = true
